name: Swift

on: [push]

jobs:
  build:

    runs-on: [macOS-latest, ubuntu-18.04]

    steps:
    - uses: actions/checkout@v1
    - name: Build
      run: swift build -v
    - name: Run tests
      run: swift test -v
